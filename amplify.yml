version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd digitwin-frontend
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: digitwin-frontend/dist
    files:
      - '**/*'
  cache:
    paths:
      - digitwin-frontend/node_modules/**/*
